// Replace with your LAN IP address.
// macOS: run `ipconfig getifaddr en0` in terminal
// Windows: run `ipconfig` and look for IPv4 Address
// The backend must be running on this address at port 3000.
export const API_URL = "http://192.168.1.204:3000";
